generator client {
  provider = "prisma-client-js"
}

datasource db {
  provider = "postgresql"
  url      = env("DATABASE_URL")
}

// Add your models here when you’re ready to use DB.
//
// Example:
// model User {
//   id        String   @id @default(cuid())
//   createdAt DateTime @default(now())
// }
